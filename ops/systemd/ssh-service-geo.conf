[Unit]
Wants=monitor-ssh-geo.service
After=monitor-ssh-geo.service

[Service]
# Reapply before every SSH daemon start, even if the guard unit was already
# active but its table was removed externally. Failure prevents this start.
ExecStartPre=/usr/local/sbin/monitor-ssh-geo-apply
